window.addEventListener('DOMContentLoaded', async () => {
  const container = document.getElementById('hyperbeam-container')
  const resp = await fetch('window.addEventListener('DOMContentLoaded', async () => {
  const container = document.getElementById('hyperbeam-container')
  const resp = await fetch('https://your-server.com/computer')
  const computer = await resp.json()
  const client = await Hyperbeam(container, computer.embed_url)
})
')
  const computer = await resp.json()
  const client = await Hyperbeam(container, computer.embed_url)
})
